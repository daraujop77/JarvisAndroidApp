<?xml version="1.0" encoding="utf-8"?>
<!--
  Screen vision stays dormant. canRetrieveWindowContent lets an approved action
  read the screen structure; the service never does it on its own. No key logging,
  no notification reading, no gesture capture beyond the one approved action.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:description="@string/screen_vision_description"
    android:notificationTimeout="100"
    android:settingsActivity="com.jarvis.android.MainActivity" />
